class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        ans = []
        temp = 0
        for i in nums:
            temp += i
            ans.append(temp)
        return ans
            
            